1. 객체와 클래스에 대한 설명으로 틀린 것 
-> 3) 하나의 클래스로 하나의 객체만 생성할 수 있다.

2. 클래스의 구성 멤버가 아닌 것
-> 4) 로컬 변수

3. 필드, 생성자, 메소드에 대한 설명으로 틀린 것
-> 4) 클래스는 반드시 필드와 메소드를 가져야 한다.

4. 필드에 대한 설명으로 틀린 것
-> 3) 필드는 반드시 생성자 선언 전에 선언되어야 한다.

5. 생성자에 대한 설명으로 틀린 것
-> 1) 객체 생성 시 생성자 호출이 반드시 필요한 것은 아니다.

6. 메소드에 대한 설명으로 틀린 것
-> 4) 메소드의 이름은 중복해서 선언할 수 없다.

7. 메소드 오버로딩에 대한 설명으로 틀린 것
-> 2) 반드시 리턴 타입이 달라야 한다.

8. 인스턴스 멤버와 정적 멤버에 대한 설명으로 틀린 것
-> 2) 인스턴스 필드는 생성자 및 정적 블록에서 초기화될 수 있다.

9. final 필드와 상수(static final)에 대한 설명으로 틀린 것
-> 2) fianl 필드와 상수는 생성자에서 초기화될 수 있다.

10. 패키지에 대한 설명으로 틀린 것
-> 4) com.mycom 패키지에 소속된 클래스는 com.yourcom에 옮겨도 동작한다.

11. 접근 제한에 대한 설명으로 틀린 것
-> 3) default 접근 제한은 해당 클래스 내부에서만 사용을 허가한다.

12. 다음 클래스에서 해당 멤버가 필드, 생성자, 메소드 중 어떤 것인지 ( ) 안에 적어보세요.
필드, 생성자, 메소드
